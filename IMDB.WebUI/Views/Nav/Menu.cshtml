@model IEnumerable<string>

@foreach(var link in Model)
{
    @Html.RouteLink(link,new { Controller = "Actor",action="List",category =link,page=1}, new
{
    @class = "btn btn-default "
 + (link == ViewBag.SelectedCategory ? " btn-primary" : "")
})
}



